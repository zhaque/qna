[buildout]
extends = buildout.cfg

eggs = 
    ${buildout:base-eggs}
    psycopg2   

parts = ${buildout:base-parts}
        pydev
        cmds

[pydev]
recipe = pb.recipes.pydev
eggs = ${buildout:eggs}
